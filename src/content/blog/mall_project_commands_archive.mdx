---
heroImage: /src/assets/images/bg.jpg
category: 学习
description: 后端配置命令
pubDate: 2024-10-17T16:00:00.000Z
draft: true
tags:
  - java
  - redis
  - mysql
title: 商城项目的配置命令存档
---

```shell
# mysql 5.7

docker run -p 3306:3306 --name mysql \
-v /volume/mysql/var/lib/mysql:/var/lib/mysql \
-v /volume/mysql/etc/mysql/conf.d:/etc/mysql/conf.d \
-v /volume/mysql/var/log/mysql:/var/log/mysql \
-e MYSQL_ROOT_PASSWORD=root \
-d mysql:5.7

touch /volume/mysql/etc/mysql/conf.d/my.cnf
vi /volume/mysql/etc/mysql/conf.d/my.cnf

# my.cnf配置文件
[client]
# 设置mysql客户端连接服务端时默认使用的端口
default-character-set=utf8
[mysql]
# 设置mysql客户端默认字符集
default-character-set=utf8
[mysqld]
# 服务端使用的字符集默认为UTF8
character-set-server=utf8
# 配置时区
default-time_zone='+8:00'





```

```shell
# redis 7.4
mkdir -p /volume/redis/etc/redis
touch /volume/redis/etc/redis/redis.conf

docker run -p 6379:6379 --name redis \
-v /volume/redis/data:/data \
-v /volume/redis/etc/redis:/etc/redis \
-d redis:7.4 redis-server /etc/redis/redis.conf
```
